# Use this for local development builds:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build

services:
  backend:
    build: ./backend
    image: ghcr.io/homebitcoinminers/bitscope-backend:local

  frontend:
    build: ./frontend
    image: ghcr.io/homebitcoinminers/bitscope-frontend:local
